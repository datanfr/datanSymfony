{# Encart d'appel aux dons (partials/campaign.php) : masqué (d-none) tant que
   `campaigns.js` n'a pas trouvé de campagne active sur
   /campaign/current_active_campaigns — la table est vide aujourd'hui, le bloc
   n'apparaît qu'une fois une campagne créée et activée au back-office.
   `classes` reprend le `wrapper_classes` de l'origine, propre à chaque page. #}
<section id="campaign" class="container-fluid d-none {{ classes|default('') }}">
  <div class="container d-flex justify-content-center flex-column align-items-center py-4 ">
    <div class="icon"><svg xmlns="http://www.w3.org/2000/svg" fill="currentColor" class="bi bi-heart-fill" viewBox="0 0 16 16">
  <path fill-rule="evenodd" d="M8 1.314C12.438-3.248 23.534 4.735 8 15-7.534 4.736 3.562-3.248 8 1.314"/>
</svg></div>
    <div id="campaign-paragraph" class="text-center my-4"></div>
    <a class="btn btn-primary" href="{{ path('page_soutenir') }}" target="_blank" rel="noopener">Faire un don</a>
  </div>
</section>
<script src="{{ asset('assets/js/datan/campaigns.js') }}" type="text/javascript"></script>
